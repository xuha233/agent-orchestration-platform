[package]
name = "aop-desktop"
version = "0.1.0"
description = "Desktop shell for Agent Orchestration Platform"
authors = ["AOP"]
license = "LGPL-3.0-only"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.0" }

[dependencies]
tauri = { version = "2.0.0", features = [] }

[features]
default = []
